// a comment

struct PakHeader
{
    char[3] fileTypeStr;
    uint8 version;
    int32 entryCount;
    uint8[248] empty;
}

struct FileDesc
{
    int32 id;
    int32 offset;
    int32 size;
}

PakHeader header;
FileDesc[header.entryCount * 2] fileDescs;
int32 test;