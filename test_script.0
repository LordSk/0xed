// a comment

struct PakHeader
{
    char[3] fileTypeStr;
    uint8 version;
    int32 entryCount;
    uint8[248] empty;
}

struct FileDesc
{
    int32 id;
    int32 offset;
    int32 size;
}

{
   {
       int64[8] test64;
   }
   float64 ftex564;
}

// PakHeader header;
// FileDesc[header.entryCount[5] * 2] fileDescs;
// int32 test;

// if((test + 1) == (1 * 4)) {
    // test = 2;
// }
// else {
    // test = 3;
// }